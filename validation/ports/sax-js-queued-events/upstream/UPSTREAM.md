# Upstream Metadata

- Repository: `isaacs/sax-js`
- Source language: JavaScript
- License: ISC
- Target style: streaming XML tokenizer with parser callbacks and queued events
- Acquisition command, if a local checkout is needed later:

```bash
git clone https://github.com/isaacs/sax-js.git validation/ports/sax-js-queued-events/upstream/sax-js
```

The current validation slice uses a documented style extraction rather than vendoring upstream source.
