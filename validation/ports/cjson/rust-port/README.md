# cJSON Rust Port

Phase 1 is limited to scalar JSON parsing: `null`, booleans, numbers, and strings. Array and object value variants exist for the eventual data model, but parser support for arrays and objects is intentionally deferred.
